name: Test and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14 
    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    - name: Set Git user info
      run: |
        git config --global user.name "HaLan0101"
        git config --global user.email "dnhalan0101@gmail.com"

    - name: Deploy to GitHub Pages
      if: success()
      run: |
        npm run build
        npx gh-pages -d build -b gh-pages -u "$GITHUB_ACTOR"
